      <div class="col-md-3">

        <h2>User info</h2>
        <%= image_tag user.get_profile_image(100,100) %>
        <table class="table">
          <tbody>
            <tr></tr>
            <tr>
              <th>name</th>
              <th><%= user.name %></th>
            </tr>
            <tr>
              <th>inroduction</th>
              <th><%= user.introduction %></th>
            </tr>
          </tbody>
        </table>
          <%= link_to edit_user_path(user), class: 'btn btn-outline-secondary btn-block' do %><i class="fas fa-user-cog"></i><% end %>


        <h2 class="mt-3">New book</h2>
        <%= form_with model: book, url: '/books', method: :post do |f| %>
        <div>
          <div class="form-group mb-3">
            <p class="mb-2">Title</p>
            <%= f.text_field :title, class: 'form-control p-1' %>
          </div>
          <div class="form-group mb-3">
            <p class="mb-2">Opinion</p>
            <%= f.text_area :body, class: 'form-control p-1p-1' %>
          </div>
          <div>
            <%= f.submit 'Create Book', class: 'btn btn-success' %>
          </div>
        </div>
        <% end %>
      </div>